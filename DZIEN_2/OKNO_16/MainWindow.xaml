<Window x:Class="WpfDay2Exercise09.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ćwiczenie 9 - DataTemplate" Height="560" Width="760">
    <Grid Margin="24">
        <StackPanel>
            <TextBlock Text="Karty zawodników" FontSize="30" FontWeight="Bold" Margin="0,0,0,20" />
            <ListBox ItemsSource="{Binding Runners}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#F8FAFC" BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="12" Padding="14" Margin="4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding FullName}" FontSize="20" FontWeight="Bold" />
                                    <TextBlock Text="{Binding Distance}" FontSize="15" Foreground="#475569" Margin="0,4,0,0" />
                                </StackPanel>

                                <Border Grid.Column="1" Background="#DBEAFE" CornerRadius="10" Padding="12,6" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Status}" FontWeight="Bold" Foreground="#1D4ED8" />
                                </Border>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </Grid>
</Window>
