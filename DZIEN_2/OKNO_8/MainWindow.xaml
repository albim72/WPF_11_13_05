<Window x:Class="DataContext_Window.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DataContext_Window"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Background="DeepSkyBlue">
        <Border Background="White" Margin="10" Width="560" 
                CornerRadius="18" Padding="28" HorizontalAlignment="Center" 
                VerticalAlignment="Center">
            <StackPanel>
                <TextBlock Text="Karta Zawodnika" FontSize="28" FontWeight="Heavy" Margin="0,0,0,16"/>
                <TextBlock Text="{Binding FullName}" FontSize="24" Foreground="BurlyWood" 
                           FontWeight="Bold" Margin="0,0,0,8"/>
                <TextBlock Text="{Binding Summary}" FontSize="18" Foreground="ForestGreen" 
                           Margin="0,0,0,20"/>
                <TextBlock Text="Ten widok czyta dane z obiektu Runner przez DataContext" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
