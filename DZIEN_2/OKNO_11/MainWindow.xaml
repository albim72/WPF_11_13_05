<Window x:Class="PropertyChanged.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PropertyChanged"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Żywy ViewModel" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,14">
            <TextBlock Text="Imię" FontSize="16" FontWeight="SemiBold"/>
            <TextBox Text="{Binding FirstName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                     FontSize="16" Height="34" />
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,14">
            <TextBlock Text="Nazwisko" FontSize="16" FontWeight="SemiBold"/>
            <TextBox Text="{Binding LastName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
             FontSize="16" Height="34" />
        </StackPanel>

        <Border Grid.Row="3" Background="BlanchedAlmond" BorderBrush="LavenderBlush" BorderThickness="2"
                CornerRadius="12" Padding="18">
            <StackPanel>
                <TextBlock Text="Aktualny podgląd" FontSize="18" FontWeight="Bold"/>
                <TextBlock Text="{Binding FullName}" FontSize="26" Margin="0,8,0,0"/>
            </StackPanel>
        </Border>

    </Grid>
</Window>
